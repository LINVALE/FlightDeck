{
  "name": "flightdeck",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FlightDeck — display and control faces for every Roon zone. A Roon add-in.",
  "license": "SEE LICENSE IN LICENSE",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node src/main.ts",
    "test": "node --test --test-concurrency=1 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "lint:floor": "node scripts/floor-lint.ts"
  },
  "dependencies": {
    "node-roon-api": "^1.2.3",
    "node-roon-api-image": "^1.0.1",
    "node-roon-api-transport": "^1.2.1"
  }
}
